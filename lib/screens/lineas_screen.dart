import 'package:flutter/material.dart';

class LineasScreen extends StatelessWidget {
   
  const LineasScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('LineasScreen'),
      ),
    );
  }
}